# Optional OpenAI key. Without it, the app runs fully offline using
# deterministic demo fallbacks. Never commit a real key.
OPENAI_API_KEY=

# Optional: model name to use for contextualization / extraction.
TOAST_OPENAI_MODEL=gpt-4o-mini
